let config = {

    mongo: {
        // url: process.env.MONGO_DB_URI || 'mongodb://localhost:27017/erp'
        url: 'mongodb://localhost/funkeDatabase'
    },

    // Secret Key
    Secret: 'Funke-Science'

};

module.exports = config;
